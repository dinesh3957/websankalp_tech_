# WebSankalp Technologies - Simple Configuration
# Basic configuration to avoid server errors

# Enable rewrite engine (optional - can be disabled if causing issues)
# RewriteEngine On

# Basic security (commented out to prevent errors)
# Options -Indexes

# Simple cache control
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 1 week"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/jpg "access plus 1 month"
</IfModule>